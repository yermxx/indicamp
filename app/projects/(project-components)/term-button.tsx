import { Button } from '@/components/ui/button';

export const TermButton = () => {
  return (
    <Button
      className='bg-blue-500 bg-opacity-10 font-bold border-none'
      variant={'outline'}
    >
      term
    </Button>
  );
};
